# AI sessions

Working artifacts from the `/propose` → `/refine` workflow. Each proposal is one
`<slug>.md` file (slug = 3–5 words from the task), capturing the plan, the tests to
write first, and the reasoning **before** implementation.

The build checklist and live status live in [`docs/implementation/`](../docs/implementation/).
